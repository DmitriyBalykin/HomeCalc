<ComboBox x:Class="HomeCalc.View.Controls.SearchComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ComboBox.ItemTemplate>
            <DataTemplate>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Name}" HorizontalAlignment="Left" Width="210"/>
                    <TextBlock Text="{Binding Date, StringFormat={}{0:yyyy-MM-dd}}" HorizontalAlignment="Right" Margin="0,0,0,10" />
                </StackPanel>
            </DataTemplate>
    </ComboBox.ItemTemplate>
</ComboBox>
