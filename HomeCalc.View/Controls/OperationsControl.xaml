<UserControl x:Class="HomeCalc.View.Controls.OperationsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/HomeCalc.View;component/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
        
        <Grid
            
            Style="{StaticResource BasicGridStyle}">
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="40" />
                <RowDefinition Height="40" />
        </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="170" />
                <ColumnDefinition Width="170" />
            </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Путь к установленной версии" />
        <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2"
            Text="{Binding ExistingPath}"/>
        <Button Grid.Row="1" Grid.Column="1"
            Content="Выбрать путь"
            Command="{Binding SelectPath}"/>
        <Button Grid.Row="1" Grid.Column="2"
            Content="Импортировать данные"
            Command="{Binding ImportData}"/>
        <Border Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Grid.ColumnSpan="3" />

        <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Новый тип покупки" />
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding NewPurchaseType}" />
        <Button Grid.Row="2" Grid.Column="2" Content="Добавить" Command="{Binding AddType}"/>

    </Grid>
        
        
</UserControl>
